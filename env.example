# =============================================================================
# Job Assistant - Environment Variables Configuration
# =============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env in the project root: cp env.example .env
# 2. Fill in the required values (marked with [REQUIRED])
# 3. Optional variables have default values and can be left as-is if desired
# 4. Never commit your .env file to version control (it's in .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# OpenAI API Configuration [REQUIRED]
# -----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# The key should start with "sk-"
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Override the default model (default: gpt-5)
# Available models depend on your OpenAI account access
OPENAI_MODEL=gpt-5

# Optional: Model for initial screening/blocker detection (default: gpt-5-mini)
# This is used for lightweight checks before expensive LLM calls
SCREENING_MODEL=gpt-5-mini

# Optional: Temperature for LLM calls (default: 0.0)
# Lower values = more deterministic, higher = more creative
# Note: Some models (gpt-5, gpt-5-preview, etc.) require temperature=1.0
LLM_TEMPERATURE=0.0

# -----------------------------------------------------------------------------
# Google Drive Configuration [REQUIRED]
# -----------------------------------------------------------------------------
# These are Google Drive folder/file IDs. To find them:
# 1. Open the folder/file in Google Drive
# 2. Look at the URL: https://drive.google.com/drive/folders/FOLDER_ID
# 3. Copy the ID (the long alphanumeric string after /folders/ or /file/d/)
#
# Folder containing your resume pointer documents (markdown/text files with
# base experience bullets and skills)
GOOGLE_DRIVE_POINTERS_FOLDER_ID=your-pointers-folder-id-here

# Folder where generated resumes will be uploaded
GOOGLE_DRIVE_OUTPUT_FOLDER_ID=your-output-folder-id-here

# Google Drive file ID for your resume template (Word document with placeholders)
# The template should have placeholders like {{CANDIDATE_NAME}}, 
# {{LEAFICIENT_EXPERIENCE_BULLET_1}}, {{TECHNICAL_SKILLS}}, etc.
RESUME_TEMPLATE_DRIVE_ID=your-template-file-id-here

# Note: You also need to place credentials.json in the project root.
# Download it from Google Cloud Console after setting up OAuth 2.0 credentials.
# The first run will create token.json automatically after OAuth flow.

# -----------------------------------------------------------------------------
# Flask Server Configuration
# -----------------------------------------------------------------------------
# Port for the Flask API server (default: 8002)
# Note: Chrome extension defaults to port 8000, so either:
# - Set this to 8000, or
# - Update SERVER_BASE_URL in chrome-extension/popup.js to match this port
FLASK_PORT=8002

# Enable Flask debug mode (default: false)
# Set to "true" for development (auto-reload, detailed errors)
# Set to "false" for production
FLASK_DEBUG=false

# -----------------------------------------------------------------------------
# Workflow Configuration
# -----------------------------------------------------------------------------
# Number of validation retries if resume validation fails (default: 2)
# The system will attempt to rewrite and validate up to this many times
VALIDATION_RETRIES=2

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# SQLite database URL for status tracking (default: sqlite:///data/status_snapshots.db)
# The database stores resume generation history and status snapshots
# You can use a different path or switch to PostgreSQL/MySQL by changing this URL
DATABASE_URL=sqlite:///data/status_snapshots.db

# =============================================================================
# Quick Setup Checklist:
# =============================================================================
# [ ] Copied env.example to .env
# [ ] Added your OPENAI_API_KEY
# [ ] Created Google Drive folders and added their IDs
# [ ] Added RESUME_TEMPLATE_DRIVE_ID
# [ ] Placed credentials.json in project root
# [ ] Set FLASK_PORT to match Chrome extension (or vice versa)
# [ ] Run: make migrate-db (to initialize database)
# [ ] Run: python run.py (to start the server)
# =============================================================================

